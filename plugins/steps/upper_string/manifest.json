{
  "id": "upper_string",
  "version": "1.0.0",
  "label": "🆙 Uppercase string",
  "description": "Convert a string column to uppercase. Hello-world plugin demo.",
  "category": "clean",
  "engine": { "primary": "sql", "browser": "sql", "deterministic": true },
  "io": {
    "inputs": { "min": 1, "max": 1, "ports": ["in"] },
    "outputs": { "min": 1, "max": 1, "ports": ["out"] }
  },
  "params": {
    "column": {
      "type": "column_ref",
      "label": "Column",
      "columnFrom": "in",
      "columnTypes": ["string"],
      "required": true
    }
  },
  "preview": { "rowImpact": "preserves", "schemaImpact": "preserves" },
  "tags": ["string", "case", "demo"]
}
