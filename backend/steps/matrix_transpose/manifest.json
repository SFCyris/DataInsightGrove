{
  "id": "matrix_transpose",
  "version": "1.0.0",
  "label": "🔁 Matrix transpose",
  "description": "Transpose a matrix. Two input shapes are supported: a column-grid (pick N numeric columns; each row becomes a column in the output) and a nested-list column (each row already carries a [[…],[…]] matrix; transpose preserves the row count).",
  "category": "shape",
  "engine": { "primary": "polars", "browser": "none", "deterministic": true },
  "io": {
    "inputs": { "min": 1, "max": 1, "ports": ["in"] },
    "outputs": { "min": 1, "max": 1, "ports": ["out"] }
  },
  "params": {
    "mode": {
      "type": "enum",
      "label": "Input shape",
      "enumValues": ["column_grid", "nested_list"],
      "default": "column_grid",
      "required": true,
      "help": "column_grid: pick N columns. The M rows × N columns form an M×N matrix; output is N rows × M columns. nested_list: each row is already a full matrix in a single nested-list column; transpose row by row."
    },
    "valueColumns": {
      "type": "column_refs",
      "label": "Value columns",
      "columnFrom": "in",
      "help": "The columns forming the matrix rows. Required for mode = column_grid. Must all be numeric.",
      "visibleWhen": { "mode": "column_grid" }
    },
    "nestedListColumn": {
      "type": "column_ref",
      "label": "Nested-list column",
      "columnFrom": "in",
      "help": "A list-of-list-of-number column. Each cell is one matrix; the result column carries each cell's transpose. Required for mode = nested_list.",
      "visibleWhen": { "mode": "nested_list" }
    },
    "outputColumn": {
      "type": "string",
      "label": "Output column name (nested_list mode)",
      "default": "transposed",
      "pattern": "^[A-Za-z_][A-Za-z0-9_ ]*$",
      "visibleWhen": { "mode": "nested_list" }
    }
  },
  "preview": { "rowImpact": "unknown", "schemaImpact": "rebuilds" },
  "tags": ["matrix", "transpose", "linalg", "shape", "rotate"],
  "requires": [{"kind": ["numeric", "array", "vector"]}],
  "aliases": ["T", "rotate matrix", "matrix.T"]
}
