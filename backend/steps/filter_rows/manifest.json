{
  "id": "filter_rows",
  "version": "1.0.0",
  "label": "🔍 Filter rows",
  "description": "Keep rows where the predicate evaluates to true. Predicate is a SQL boolean expression over column names.",
  "category": "shape",
  "engine": { "primary": "sql", "browser": "sql", "deterministic": true },
  "io": {
    "inputs": { "min": 1, "max": 1, "ports": ["in"] },
    "outputs": { "min": 1, "max": 1, "ports": ["out"] }
  },
  "params": {
    "predicate": {
      "type": "expression",
      "widget": "filter_builder",
      "label": "Keep rows where",
      "help": "Build conditions visually, or switch to SQL mode for full control.",
      "required": true
    }
  },
  "preview": { "rowImpact": "may_reduce", "schemaImpact": "preserves" },
  "tags": ["filter", "where"],
  "aliases": ["where", "predicate", "condition", "subset by"]
}
