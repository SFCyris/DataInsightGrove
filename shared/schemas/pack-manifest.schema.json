{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://datainsightgrove.local/schemas/pack-manifest.v1.json",
  "title": "DIG Step-Pack Manifest",
  "description": "Manifest for a step pack — a distributable bundle of step (and optionally connector) plugins. Lives at the root of the pack archive as `pack.json`. The runtime validates this file before any code is loaded.",
  "type": "object",
  "required": ["id", "version", "label", "description"],
  "additionalProperties": false,
  "properties": {
    "id": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9_]*$",
      "minLength": 2,
      "maxLength": 40,
      "description": "Stable, unique pack identifier. snake_case. Must match the pack's top-level directory name inside the .dpack archive."
    },
    "version": {
      "type": "string",
      "pattern": "^\\d+\\.\\d+\\.\\d+$",
      "description": "Semantic version of the pack. The .dpack filename should embed this version (e.g. `statspack-1.0.0.dpack`)."
    },
    "label": {
      "type": "string",
      "minLength": 1,
      "maxLength": 80,
      "description": "Human-readable display name shown in the Step-Packs UI. House style: emoji prefix."
    },
    "description": {
      "type": "string",
      "minLength": 1,
      "maxLength": 500,
      "description": "One-paragraph description of what the pack adds. Surfaced in the install dialog."
    },
    "author": {
      "type": "string",
      "maxLength": 120,
      "description": "Pack author or maintainer org."
    },
    "license": {
      "type": "string",
      "maxLength": 80,
      "description": "SPDX license identifier (e.g. AGPL-3.0, MIT, Apache-2.0)."
    },
    "homepage": {
      "type": "string",
      "maxLength": 500,
      "description": "Optional URL to the pack's homepage / source / docs."
    },
    "minDigVersion": {
      "type": "string",
      "pattern": "^\\d+\\.\\d+\\.\\d+$",
      "description": "Minimum DIG host version this pack expects. Loader rejects packs whose minDigVersion is greater than the running backend's version."
    },
    "steps": {
      "type": "array",
      "items": { "type": "string", "pattern": "^[a-z][a-z0-9_]*$" },
      "uniqueItems": true,
      "description": "Declared list of step IDs the pack ships. Must match the directories under `steps/` in the archive."
    },
    "connectors": {
      "type": "array",
      "items": { "type": "string", "pattern": "^[a-z][a-z0-9_]*$" },
      "uniqueItems": true,
      "description": "Declared list of connector IDs the pack ships. Must match the directories under `connectors/` in the archive."
    },
    "pythonRequirements": {
      "type": "array",
      "maxItems": 64,
      "items": {
        "type": "string",
        "minLength": 1,
        "maxLength": 200,
        "pattern": "^[A-Za-z][A-Za-z0-9._-]{0,63}(\\[[A-Za-z0-9_,\\-\\s]{1,80}\\])?(\\s*(==|!=|<=|>=|<|>|~=|===)\\s*[A-Za-z0-9._\\-+*]{1,64}(\\s*,\\s*(==|!=|<=|>=|<|>|~=|===)\\s*[A-Za-z0-9._\\-+*]{1,64})*)?(\\s*;\\s*[A-Za-z0-9_\\s.<>=!'\"()\\-]{1,160})?\\s*$",
        "description": "PEP 508 requirement string — name[extras][specifier][;marker]. URL/VCS/path forms (`git+`, `pkg @ url`, `./local`, `-e`) and pip flags (anything starting with `-`) are rejected to prevent supply-chain redirection."
      },
      "description": "Pip-style requirements (e.g. `scipy>=1.11`). When auto-install is enabled (DIG_PACK_AUTO_INSTALL_DEPS), each entry is validated against the strict subset above and installed via `pip install --isolated --index-url https://pypi.org/simple/ -- <reqs>`. Operators can override the index with DIG_PIP_INDEX_URL."
    },
    "checksum": {
      "type": "string",
      "pattern": "^sha256:[a-f0-9]{64}$",
      "description": "sha256 of the archive contents, stamped at build time by build_pack.py. Used by the install registry for integrity verification."
    },
    "signature": {
      "type": "string",
      "description": "Optional ed25519 signature of `checksum` by the pack publisher. Reserved for v2; current builds may omit."
    },
    "metadata": {
      "type": "object",
      "additionalProperties": true,
      "description": "Free-form pack/author metadata not interpreted by DIG core."
    },
    "extensions": {
      "type": "object",
      "additionalProperties": true,
      "description": "Namespaced extension fields. Vendors / enterprise / forks attach their own data under a top-level namespace key (e.g. extensions.acme.distributionChannel) so future first-party fields never collide."
    },
    "resources": {
      "type": "object",
      "description": "Bundled reference datasets shipped with the pack. The pack archive contains a single `archive` zip; on first use the loader verifies the archive's sha256, extracts it under data/cache/pack_data/<pack_id>/<version>/, and exposes the files via dig.extensions.pack_data.pack_data(pack_id, resource_id). The archive ships flat (no nested directories), one entry per file declared in `files`. Step code never accepts a raw filesystem path from pipeline params — only resource ids declared here — so unbundled files on disk cannot be read through a step.",
      "required": ["archive", "sha256", "files"],
      "additionalProperties": false,
      "properties": {
        "archive": {
          "type": "string",
          "pattern": "^[a-zA-Z0-9_.-]+\\.zip$",
          "description": "Filename of the zip living at the pack's root (typically `data.zip`)."
        },
        "sha256": {
          "type": "string",
          "pattern": "^[a-f0-9]{64}$",
          "description": "Sha256 of the archive bytes. Verified before extraction."
        },
        "compressedBytes": {
          "type": "integer",
          "minimum": 0,
          "description": "Size of the on-disk archive in bytes."
        },
        "extractedBytes": {
          "type": "integer",
          "minimum": 0,
          "description": "Sum of file_size of every entry in the archive. Compared against the loader's hard cap (default 200 MB) before extraction starts."
        },
        "files": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "object",
            "required": ["id", "path", "license"],
            "additionalProperties": false,
            "properties": {
              "id": {
                "type": "string",
                "pattern": "^[a-z][a-z0-9_]*$",
                "description": "Stable identifier the step code looks up via pack_data(pack_id, id)."
              },
              "path": {
                "type": "string",
                "pattern": "^[a-zA-Z0-9_.-]+$",
                "description": "Filename inside the archive. Flat — no nested dirs. The pack-data loader rejects any path containing a slash to defeat zip-slip."
              },
              "sha256": {
                "type": "string",
                "pattern": "^[a-f0-9]{64}$",
                "description": "Optional per-file sha256, checked the first time pack_data() resolves this resource. Lets the loader detect corruption inside the extracted tree without re-hashing the whole archive."
              },
              "sizeBytes": { "type": "integer", "minimum": 0 },
              "rowCount": { "type": "integer", "minimum": 0 },
              "schemaVersion": { "type": "integer", "minimum": 1 },
              "license": {
                "type": "string",
                "description": "SPDX identifier or short license name. Surfaced in Settings → Packs and in pipeline export attribution chains."
              },
              "attribution": { "type": "string", "description": "One-line credit string." },
              "source": { "type": "string", "description": "Where the data came from — URL, dataset name, vendor, internal team contact." },
              "description": { "type": "string", "maxLength": 500 }
            }
          }
        }
      }
    }
  }
}
