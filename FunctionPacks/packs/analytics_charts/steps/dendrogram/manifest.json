{
  "id": "dendrogram",
  "version": "1.0.0",
  "label": "🌲 Dendrogram",
  "description": "Hierarchical clustering tree from a vector column. Use to discover natural groupings — gene expression cohorts, customer segments, document similarity. Pair with the built-in distance_matrix step when you have pairwise distances rather than feature vectors.",
  "category": "visualize",
  "engine": { "primary": "polars", "browser": "none", "deterministic": true },
  "io": {
    "inputs": { "min": 1, "max": 1, "ports": ["in"] },
    "outputs": { "min": 1, "max": 1, "ports": ["out"] }
  },
  "params": {
    "vectorColumn": { "type": "column_ref", "label": "Vector column", "columnFrom": "in", "required": true,
      "help": "List-of-numbers column. Each row is one observation; the dendrogram clusters across rows." },
    "labelColumn": { "type": "column_ref", "label": "Label column (optional)", "columnFrom": "in",
      "help": "Used as leaf labels. Defaults to integer row index." },
    "method": {
      "type": "enum", "label": "Linkage method",
      "enumValues": ["ward", "average", "complete", "single"],
      "default": "ward",
      "help": "ward (default): minimises within-cluster variance. average: UPGMA. complete: max distance. single: chain-style nearest-neighbour."
    },
    "metric": {
      "type": "enum", "label": "Distance metric",
      "enumValues": ["euclidean", "cosine", "manhattan"],
      "default": "euclidean",
      "help": "Distance between observations. 'ward' linkage requires euclidean; using a non-euclidean metric with ward falls back to complete."
    },
    "title": { "type": "string", "label": "Title", "default": "Dendrogram" },
    "format": { "type": "enum", "label": "Format", "enumValues": ["png", "svg"], "default": "png", "required": true },
    "width":  { "type": "integer", "label": "Width (px)",  "default": 1600, "min": 200, "max": 4000 },
    "height": { "type": "integer", "label": "Height (px)", "default": 800,  "min": 200, "max": 4000 }
  },
  "preview": { "rowImpact": "preserves", "schemaImpact": "preserves" },
  "tags": ["chart", "hierarchical", "clustering", "tree", "linkage"],
  "requires": [{"kind": ["array", "vector"]}],
  "aliases": ["hierarchical clustering", "linkage tree", "agglomerative"]
}
