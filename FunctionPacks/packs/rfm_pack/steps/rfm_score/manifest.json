{
  "id": "rfm_score",
  "version": "1.0.0",
  "label": "🎯 RFM score",
  "description": "Compute Recency / Frequency / Monetary scores per customer from a transaction log. Each axis is bucketed into N quantile bins (default 5); the per-customer output is one row carrying r_score, f_score, m_score (1=worst, N=best) plus the combined rfm_segment string (e.g. \"5-5-5\" = champions).",
  "category": "aggregate",
  "engine": { "primary": "polars", "browser": "none", "deterministic": true },
  "io": {
    "inputs": { "min": 1, "max": 1, "ports": ["in"] },
    "outputs": { "min": 1, "max": 1, "ports": ["out"] }
  },
  "params": {
    "customerColumn": { "type": "column_ref", "label": "Customer ID column", "columnFrom": "in", "required": true },
    "dateColumn":     { "type": "column_ref", "label": "Transaction date column", "columnFrom": "in", "required": true },
    "amountColumn":   { "type": "column_ref", "label": "Transaction amount column", "columnFrom": "in", "required": true,
                         "columnTypes": ["integer", "double"] },
    "asOfDate": {
      "type": "string",
      "label": "As-of date (YYYY-MM-DD; today if blank)",
      "help": "Recency = days between this date and the customer's most recent transaction."
    },
    "bins": { "type": "integer", "label": "Number of quantile bins per axis", "default": 5, "min": 3, "max": 10 }
  },
  "preview": { "rowImpact": "may_reduce", "schemaImpact": "rebuilds" },
  "tags": ["rfm", "segmentation", "marketing", "customer", "cohort"],
  "aliases": ["recency frequency monetary", "customer scoring", "RFM segmentation"],
  "requires": [{"kind": "any"}, {"kind": "temporal"}, {"kind": "numeric"}]
}
