{
  "id": "ecdf_plot",
  "version": "1.0.0",
  "label": "📊 ECDF plot",
  "description": "Empirical Cumulative Distribution Function — every data point gets a y-value showing what fraction of the sample is ≤ it. Optionally split by a categorical group column to compare distributions.",
  "category": "visualize",
  "engine": { "primary": "polars", "browser": "none", "deterministic": true },
  "io": {
    "inputs": { "min": 1, "max": 1, "ports": ["in"] },
    "outputs": { "min": 1, "max": 1, "ports": ["out"] }
  },
  "params": {
    "value": {
      "type": "column_ref", "label": "Value column",
      "columnFrom": "in", "required": true,
      "columnTypes": ["integer", "double"]
    },
    "group": {
      "type": "column_ref", "label": "Group column (optional)", "columnFrom": "in"
    },
    "title": {
      "type": "string", "label": "Title", "default": "ECDF"
    },
    "output_path": {
      "type": "string", "label": "Output path"
    }
  },
  "preview": { "rowImpact": "preserves", "schemaImpact": "preserves" },
  "tags": ["chart", "diagnostic"],
  "aliases": ["empirical cdf", "cumulative distribution"],
  "requires": [{"kind": "numeric"}]
}
